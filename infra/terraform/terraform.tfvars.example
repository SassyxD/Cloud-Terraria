# Terraform Variables Configuration
# สร้างไฟล์นี้ใน infra/terraform/terraform.tfvars

# ข้อมูลพื้นฐาน
project = "terraria"
region  = "ap-southeast-1"  # เปลี่ยนเป็น region ที่ต้องการ

# เปิดใช้งาน RDS
enable_rds = true

# การตั้งค่า RDS
rds_instance_class          = "db.t3.micro"     # Free tier eligible
rds_allocated_storage       = 20                # GB (ขั้นต่ำ)
rds_max_allocated_storage   = 100               # GB (Auto-scaling)
rds_username                = "postgres"
rds_password                = "ChangeThisPassword123!"  # ⚠️ เปลี่ยนเป็นรหัสผ่านที่แข็งแรง!

# ความปลอดภัย
allow_rds_public_access     = false             # ไม่อนุญาตให้เข้าถึงจาก internet
rds_deletion_protection     = true              # ป้องกันการลบโดยไม่ตั้งใจ
rds_backup_retention_days   = 7                 # เก็บ backup 7 วัน
rds_skip_final_snapshot     = false             # สร้าง snapshot ก่อนลบ

# EC2 Instance (สำหรับ Terraria servers)
instance_type               = "t3.small"        # ขนาด server
